# flutter_gen configuration
# Documentation: https://pub.dev/packages/flutter_gen

flutter_gen:
  # Output directory for generated files
  output: lib/gen/

  # Integration settings
  integrations:
    flutter_svg: true

  # Assets generation
  assets:
    # Define the assets to be included
    enabled: true
    outputs:
      package_parameter_enabled: true
